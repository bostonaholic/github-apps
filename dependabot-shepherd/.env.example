# Copy to .env and fill in after registering the app.
# Leave empty and run `npm start` to use Probot's setup flow instead.
APP_ID=
WEBHOOK_SECRET=
PRIVATE_KEY=
# For local development: a channel from https://smee.io/new
WEBHOOK_PROXY_URL=
